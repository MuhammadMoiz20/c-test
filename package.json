{
  "name": "c-test",
  "version": "1.0.0",
  "private": true,
  "description": "Basic static app with Cypress tests",
  "scripts": {
    "serve": "http-server ./src -p 5173 -a 127.0.0.1 -c-1",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "start-server-and-test serve http://127.0.0.1:5173 cy:run"
  },
  "devDependencies": {
    "cypress": "^15.5.0",
    "http-server": "^14.1.1",
    "start-server-and-test": "^2.1.2"
  }
}
